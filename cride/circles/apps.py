"""Circles app configuration."""

# Django
from django.apps import AppConfig

class CirclesConfig(AppConfig):
    """CirclesConfig class that inherits from AppConfig.

    Provides the necessary data for this app to be considered as one.
    """
    name = 'cride.circles'
    verbose_name = 'Circles'
